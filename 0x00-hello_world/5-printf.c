#include <stdio.h>
/**
 * main - program a printing line using print function with return value 0
 * return is integer: 0 (success)
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
